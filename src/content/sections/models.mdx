---
order: 6
anchor: models
nav: "Your model"
label: "Whichever model you trust"
title: "Any model."
titleSecond: "Including the one on your laptop."
lede: "The model never sees your rows — only what the measurements came back with. Which is why you do not need the biggest one."
---

The body of this file is not rendered. Two rails: hosted providers, then the
local runtimes on their own row, because the claim about them is different.
