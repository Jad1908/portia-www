---
order: 0
anchor: top
label: "Pre-launch"
title: "It learns your data."
titleSecond: "And it doesn't forget."
lede: "A copilot for data scientists. portia understands what your data sources are, keeps what it learns, and builds the pipeline with you."
---

The body of this file is not rendered. The hero is two lines and a sentence.

`title` and `titleSecond` are separate fields because the second line is set in
`mute` against the first line's `ink`, and because each line rises out of its
own clipped box — so the split has to exist in the data, not in a `<br>`.
