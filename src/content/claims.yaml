# Short lines of consequence, grouped by the section they sit in.
#
# `lead` is set in `ink` and `rest` in `body` — one sentence, split so the claim
# reads before the qualification does. Two or three per section is the budget.
#
# **Keep `rest` to one short sentence.** Two sentences per claim is how this
# file drifts back into prose, and the tone that comes with it is a page
# defending itself. Say the thing and stop.
#
# No claim here names a file format, a schema or a library.

# --- Warehouses -------------------------------------------------------------

- id: claim-1
  group: warehouse
  lead: "Nothing gets copied."
  rest: "No extract, no staging table, no second version to keep in sync."

- id: claim-2
  group: warehouse
  lead: "Size is not your problem."
  rest: "A table too big to open is one portia still knows."

# --- Models -----------------------------------------------------------------

- id: claim-3
  group: models
  lead: "Bring your own."
  rest: "The provider you already pay for, or none at all."

- id: claim-4
  group: models
  lead: "Small models are enough."
  rest: "The context does the work, so your bill stays small too."
